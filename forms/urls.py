from rest_framework import routers
# from forms.views import FormViewSet, VersionViewSet, AnswerViewSet


router = routers.SimpleRouter()
# router.register(r'forms', FormViewSet, base_name='forms')
# router.register(r'versions', VersionViewSet, base_name='versions')
# router.register(r'answers', AnswerViewSet, base_name='answers')

urlpatterns = [
]
